This package contains the code and data for {citation}.

The main simulation code can be found in run_sims.py which can  be used with command line arguments and flexArgs for different  network types

The figures are generated in the make_all_figures_NotebookVersion.ipynb Jupiter Notebook using the data in data/results and are stored in figures